// Runtime config for the no-build static frontend. Loaded BEFORE app.js.
// After the backend is deployed to Railway, replace the value below with the
// public Railway HTTPS URL (no trailing slash), e.g.
//   API_URL: 'https://bskim-portfolio-production.up.railway.app'
window.APP_CONFIG = {
  API_URL: 'https://bskim-api-production.up.railway.app'
};
